# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page

POST        /create/campaign/pricelist/csv/google        controllers.google.PriceListController.createAdsCampaignFromCsvPriceList(clientId: String ?= "8705483891")

POST        /upload/csv                                  controllers.Uploader.uploadCsvFile
POST        /upload/csvs                                 controllers.Uploader.uploadCsvFiles


# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                                controllers.Assets.at(path="/public", file)